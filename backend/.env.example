# PAW-PK Backend Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
NODE_ENV=production
PORT=3001
BACKEND_PORT=3001

# Frontend URL (for CORS) - Replace with your actual domain
FRONTEND_URL=https://yourdomain.com

# JWT Configuration - Generate a secure secret:
# openssl rand -base64 32
JWT_SECRET=your-secure-random-secret-key-here
JWT_EXPIRES_IN=7d

# Database (SQLite)
DB_FILE=./pawpk_prod.sqlite3

# Admin Credentials (used by seed script)
# Change these before running seeds in production
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=change-this-secure-password
