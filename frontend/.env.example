# PAW-PK Frontend Environment Configuration
# Copy this file to .env.production and update the values

# API URL - Use relative path when frontend & backend are on same server
# For VPS deployment (recommended):
VITE_API_URL=/api

# For separate servers (if backend is on different domain):
# VITE_API_URL=https://api.yourdomain.com/api
